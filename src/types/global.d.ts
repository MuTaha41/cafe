interface RestaurantData {
  name: string;
  address: string;
  cuisine: string;
  rating: number;
  isOpen: boolean;
}

interface Restaurant {
  id: string;
  name: string;
  cuisine: string;
  location: string;
}

